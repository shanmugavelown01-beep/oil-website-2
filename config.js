// Environment Configuration (restored to working local-default)
// This file intentionally uses the local Express backend by default.
// If you deploy the backend to a public URL, update `apiUrl` here.

const CONFIG = {
  apiUrl: 'http://localhost:3000'
};

// Debug log
console.log('CONFIG:', CONFIG);
